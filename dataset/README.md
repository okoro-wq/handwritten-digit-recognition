# Dataset

MNIST is fetched automatically by Keras (`keras.datasets.mnist.load_data()`) into
`~/.keras/datasets` the first time `model/train.py` runs. It's not vendored in this
repo, so this directory is intentionally empty.
